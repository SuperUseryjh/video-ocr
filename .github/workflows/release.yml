name: Build and publish Windows package

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "要发布的标签，必须与 version.py 对应，例如 v0.1.0"
        required: true
        type: string
permissions:
  contents: read

jobs:
  publish:
    runs-on: windows-latest
    env:
      STATIC_REPOSITORY: SuperUseryjh/static
      STATIC_PATH: video-ocr
      RELEASE_TAG: ${{ inputs.release_tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install build dependencies
        run: python -m pip install -r requirements-build.txt

      - name: Validate release version
        shell: pwsh
        run: |
          $version = python -c "from video_ocr.version import __version__; print(__version__)"
          if ("v$version" -ne $env:RELEASE_TAG) { throw "Release tag must equal v$version" }

      - name: Download FFmpeg
        shell: pwsh
        run: |
          Invoke-WebRequest "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile ffmpeg.zip
          Expand-Archive ffmpeg.zip ffmpeg
          $ffmpeg = Get-ChildItem ffmpeg -Recurse -Filter ffmpeg.exe | Select-Object -First 1
          New-Item -ItemType Directory -Force bin | Out-Null
          Copy-Item $ffmpeg.FullName bin/ffmpeg.exe

      - name: Build application package
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --onedir --windowed --name VideoOCR --add-binary "bin/ffmpeg.exe;bin" --collect-all rapidocr --collect-all onnxruntime main.py
          New-Item -ItemType Directory -Force package | Out-Null
          Move-Item dist/VideoOCR package/VideoOCR
          Compress-Archive -Path package/VideoOCR -DestinationPath "VideoOCR-windows-x64-$env:RELEASE_TAG.zip"
          $hash = (Get-FileHash "VideoOCR-windows-x64-$env:RELEASE_TAG.zip" -Algorithm SHA256).Hash.ToLower()
          $partSize = 20MB
          $source = [System.IO.File]::OpenRead("VideoOCR-windows-x64-$env:RELEASE_TAG.zip")
          $buffer = New-Object byte[] $partSize
          $parts = @()
          $index = 1
          try {
            while (($read = $source.Read($buffer, 0, $buffer.Length)) -gt 0) {
              $partName = "VideoOCR-windows-x64-$env:RELEASE_TAG.zip.part{0:D3}" -f $index
              $destination = [System.IO.File]::Create($partName)
              try { $destination.Write($buffer, 0, $read) } finally { $destination.Dispose() }
              $parts += "https://static.yaoonion.fun/$env:STATIC_PATH/$partName"
              $index++
            }
          } finally { $source.Dispose() }
          "$hash  VideoOCR-windows-x64-$env:RELEASE_TAG.zip" | Set-Content SHA256SUMS.txt -NoNewline
          @{ version = $env:RELEASE_TAG.TrimStart("v"); parts = $parts; sha256 = $hash } | ConvertTo-Json | Set-Content latest.json

      - uses: actions/checkout@v4
        with:
          repository: SuperUseryjh/static
          token: ${{ secrets.STATIC_REPO_PAT }}
          path: static

      - name: Publish static assets
        shell: pwsh
        run: |
          $publishDirectory = "static/$env:STATIC_PATH"
          if (Test-Path $publishDirectory) { Remove-Item $publishDirectory -Recurse -Force }
          New-Item -ItemType Directory -Force $publishDirectory | Out-Null
          Copy-Item "VideoOCR-windows-x64-$env:RELEASE_TAG.zip.part*" $publishDirectory
          Copy-Item SHA256SUMS.txt $publishDirectory
          Copy-Item latest.json $publishDirectory
          Set-Location static
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$env:STATIC_PATH"
          if (git diff --cached --quiet) { exit 0 }
          git commit -m "Publish VideoOCR $env:RELEASE_TAG"
          git push
